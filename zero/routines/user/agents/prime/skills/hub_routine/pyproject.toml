[project]
name = "hub_routine"
version = "0.1.0"
description = "zero routine skill: IPython 里一键启动 routine hub + passive routine"
requires-python = ">=3.11"
dependencies = ["routine"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hub_routine"]
